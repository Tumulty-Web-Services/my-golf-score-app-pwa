import React from 'react';
interface AvatarProps {
    /** specify icon="search" or icon={IconComponent} */
    icon: React.ReactNode | string;
}
export declare const component: ({ icon }: AvatarProps) => JSX.Element;
export {};
